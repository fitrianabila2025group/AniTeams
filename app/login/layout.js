export const metadata = {
  title: "Login",
  description: "Log in to your AniTeams account to access your watchlist and bookmarks.",
  alternates: {
    canonical: "/login",
  },
};

export default function LoginLayout({ children }) {
  return children;
}
