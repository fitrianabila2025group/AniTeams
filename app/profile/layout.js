export const metadata = {
  title: "My Profile",
  description: "View your AniTeams profile and anime list synced with AniList.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return children;
}
